import React from "react";
import { Link } from "../components/Category/styles";
import { Layout } from "../components/Layout";

const NotFound = () => {
  return <Layout title={"Página incorrecta"} showTitle={true} />;
};

export default NotFound;
